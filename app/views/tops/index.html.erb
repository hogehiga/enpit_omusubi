<h1>えんむすび</h1>

これはインスタ映えしそう！！　今すぐ行け！！！！<br>
画像を選択してルート検索を押すと、那覇空港から出発していい感じにまわっていくルートを提示します！！！

<% if @navigate_form.errors.any? %>
    <% @navigate_form.errors.full_messages.each do |msg| %>
        <p class="warn"><%= msg %></p>
    <% end %>
<% end %>

<%= form_with model: @navigate_form, url: '/', method: :post, local: true do |f| %>
    <div class="container">
        <% @images.each do |image| %>
            <% next unless image.image_path %>
            <div class="item">
                <% if @navigate_form.images && @navigate_form.images["#{image.id}"] != "0" %>
                    <%= image_tag("/#{image.image_path}",:class => "thumbnail checked") %>
                    <%= f.check_box "images[#{image.id}]", { class: "disabled_checkbox", checked: true } %>
                <% else %>
                    <%= image_tag("/#{image.image_path}",:class => "thumbnail") %>
                    <%= f.check_box "images[#{image.id}]", { class: "disabled_checkbox" } %>
                <% end %>
            </div>
        <% end %>
        <div class="route_button_wrapper">
            <%= f.submit "ルート検索", { class: 'route_button'} %>
        </div>
    </div>
<% end %>
