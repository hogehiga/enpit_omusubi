<h1>えんむすび</h1>

これはインスタ映えしそう！！　今すぐ行け！！！！
画像をクリックするとmapが表示されます。

<div class="container">
    <% @images.each do |image| %>
        <div class="item">
            <%= image_tag("/#{image.image_path}",:class => "thumbnail") %>
            <input type="checkbox" name="<%= image.image_path %>"class="disabled_checkbox" checked>
        </div>
    <% end %>
</div>
